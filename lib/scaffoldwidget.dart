import 'package:flutter/material.dart';

class scafwid extends StatelessWidget {
  const scafwid({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
      drawer: Drawer(),
    );
  }
}

